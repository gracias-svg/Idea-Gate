node_modules
.next
.env
.env.local
.env.production
.DS_Store
coverage
dist
build
*.log
/workspace/
*.backup
*old.ts
*older*.ts
tsconfig.tsbuildinfo
ui-layer@*
.claude/

# local backups / build output — never commit
CLAUDE.md.backup-*
backup-before-claude-files/
/next
*(old).tsx
*(old).ts
