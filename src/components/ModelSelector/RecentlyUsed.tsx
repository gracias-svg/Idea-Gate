'use client';
// src/components/ModelSelector/RecentlyUsed.tsx
// Mission: 12A — STUB ONLY. Not rendered, not wired, not functional in this mission.
// Reserved per Spec Section 16 (Future Extensibility) so the folder structure does not
// need restructuring when Recently Used is actually implemented in a later mission.

export function RecentlyUsed() {
  return null;
}
