import { BaseAgent } from './base-agent.js';

export class ProductStrategyAgent extends BaseAgent {
  constructor() {
    super({
      name: "ProductStrategyAgent",
      role: "Senior Product Manager at a top tech company"
    });
  }
}